{
  "name": "server",
  "version": "1.0.0",
  "description": "simple server for angular-cli project by typescript",
  "main": "../dist/server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "cd .. && ng build && cd server && tsc",
    "run" : "cd ../dist/server && nodemon index"
  },
  "keywords": [
    "angular-cli",
    "typescript",
    "server",
    "simple-server"
  ],
  "author": "xanhtool",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2"
  },
  "devDependencies": {
    "@types/body-parser": "^1.16.3",
    "@types/express": "^4.0.35"
  }
}
